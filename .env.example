# Used by docker-compose.yml (docker compose automatically reads .env in
# this directory). Not the same file as backend/.env, which is for running
# the backend directly on the host with macOS/Homebrew paths.
HA_TOKEN=
